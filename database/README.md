# CSDL Website Tin Tức

Import lần lượt `schema.sql`, `seed.sql`, rồi chạy `queries.sql` trong phpMyAdmin. Hoặc dùng MySQL CLI:

```bash
mysql -u root -p < schema.sql
mysql -u root -p < seed.sql
mysql -u root -p < queries.sql
```

ERD: `practice/bai-tap-tren-lop/buoi-04/bai-01.php`.
